/**
 * Base URL of the main site Petri is deployed alongside (as a subdomain).
 * chiang.ink's own DESIGN.md pins this to the `www` host — never `chiangs.ink`.
 */
export const MAIN_SITE_URL = 'https://www.chiang.ink'
