import type { ExperimentMeta } from "@/lib/types";

const meta: ExperimentMeta = {
  title: "Skeleton loader",
  description:
    "A lean skeleton primitive with a single theme-agnostic gradient sweep, composed to trace a real card.",
  tags: ["loading", "animation"],
  complexity: "simple",
};

export default meta;
