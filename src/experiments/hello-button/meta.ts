import type { ExperimentMeta } from '../../lib/types'

const meta: ExperimentMeta = {
  title: 'Hello Button',
  description: 'The smallest possible experiment: a button with local state.',
  tags: ['interaction', 'state'],
}

export default meta
