import type { ExperimentMeta } from '@/lib/types'

const meta: ExperimentMeta = {
  title: 'Draggable dashboard',
  description:
    'A data dashboard where every widget drags freely across the canvas and can optionally snap to a grid.',
  complexity: 'complex',
  tags: ['dashboard', 'drag-and-drop', 'layout', 'grid'],
}

export default meta
