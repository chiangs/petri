import type { ExperimentMeta } from '@/lib/types'

const meta: ExperimentMeta = {
  title: 'Draggable dashboard',
  description:
    'An enterprise dashboard shell whose widgets drag freely across the canvas and can optionally snap to a grid.',
  complexity: 'complex',
  tags: ['dashboard', 'drag-and-drop', 'layout', 'grid', 'navigation'],
}

export default meta
