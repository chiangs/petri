// The dashboard shell mocks a product with its own brand, so its nav accent is
// an intentional off-token colour (vibrant blue-purple) rather than a petri
// design token. See README → Best-practice note. Applied via inline style /
// SVG attributes so there's one source of truth (Tailwind arbitrary classes
// would need the literal hex repeated).
export const NAV_ACCENT = '#6E56F8'
